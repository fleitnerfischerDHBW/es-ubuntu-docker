name: publish
on: [push]
jobs:
publish-es-ubuntu-docker-image:
runs-on: ubuntu-latest
steps:
- uses: actions/checkout@v2
- name: Login to GitHub Container Registry
uses: docker/login-action@v1
with:
registry: ghcr.io
username: ${{ github.actor }}
password: ${{ secrets.GITHUB_TOKEN }}
- name: Build the es-ubuntu-docker Docker image
run: |
       docker build . --tag ghcr.io/fleitnerfischerDHBW/es-ubuntu-docker:latest
       docker run ghcr.io/fleitnerfischerDHBW/es-ubuntu-docker:latest
       docker push ghcr.io/fleitnerfischerDHBW/es-ubuntu-docker:latest